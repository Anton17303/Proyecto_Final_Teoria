machine:
q_states:
q_list: ['q0', 'q_seek_b', 'q_return', 'q_accept', 'q_reject']
initial: 'q0'
final: ['q_accept']

alphabet:
input: ['a', 'b']
tape: ['a', 'b', 'X', '#']

blank: '#'

delta: []

machine:
q_states:
q_list: ['q0', 'q_seek_b', 'q_return', 'q_accept', 'q_reject']
initial: 'q0'
final: ['q_accept']

alphabet:
input: ['a', 'b']
tape: ['a', 'b', 'X', '#']

blank: '#'

delta:
# q0: buscar primera 'a' sin convertir
- params:
initial_state: 'q0'
tape_input: 'a'
output:
final_state: 'q_seek_b'
tape_output: 'X'
tape_displacement: 'R'

```
# q0 encuentra 'X', sigue buscando nuevas 'a'
- params:
    initial_state: 'q0'
    tape_input: 'X'
  output:
    final_state: 'q0'
    tape_output: 'X'
    tape_displacement: 'R'

# Si en q0 se encuentra un 'b' → error
- params:
    initial_state: 'q0'
    tape_input: 'b'
  output:
    final_state: 'q_reject'
    tape_output: 'b'
    tape_displacement: 'S'

# Si q0 encuentra fin → aceptar
- params:
    initial_state: 'q0'
    tape_input: '#'
  output:
    final_state: 'q_accept'
    tape_output: '#'
    tape_displacement: 'S'
```
